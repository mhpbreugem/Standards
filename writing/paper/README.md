# Paper structure

Standard for manuscript layout. **Planned — not yet written.**

Will cover: the paper repository layout (where `figures/`, `tables/`, data, and
build files live), section order and naming, the LaTeX build, and how a paper wires
in this hub's `writing/`, `methods/`, and `runner/` standards (vendor vs submodule,
the pointer block for the paper's own `CLAUDE.md`).
